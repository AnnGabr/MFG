<resources>

    <!-- Base application theme. -->
    <style name="GreenLightBlueTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/colorPrimary1</item>
        <item name="colorPrimary">@color/colorPrimaryDark1</item>
        <item name="colorAccent">@color/colorAccent1</item>
        <item name="android:colorBackground">@color/colorBackground1</item>
        <item name="android:statusBarColor">@color/colorBars1</item>
        <item name="android:navigationBarColor">@color/colorBars1</item>
        <item name="android:windowBackground">@color/colorBackground1</item>
        <item name="android:textColor">@color/colorText1</item>
        <item name="android:colorButtonNormal">@color/colorBtn1</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textColorPrimaryInverse">@android:color/white</item>
    </style>

</resources>
